# LoadTest.md — ロードテスト / ヒートラン手順

CardputerRadiko の品質確認（連続再生の安定性・発熱・メモリリーク・切断耐性）のための手順です。
ファームには **60 秒ごとの健全性ログ** を組み込んであります。シリアルモニタ（115200 baud）で観測します。

```
pio device monitor            # または: ...\pio.exe device monitor
```

---

## 健全性ログの読み方

60 秒ごとに次の行が出ます。

```
[health] up=3600s heap=142000 min=118000 temp=44.2C rssi=-58 wifi=OK drops=0 state=再生中 under=0 starve=2 bat=63+CHG
```

| 項目 | 意味 | 見るポイント |
|---|---|---|
| `up` | 起動からの経過秒 | 連続稼働時間 |
| `heap` | 現在の空きヒープ(byte) | 大きく単調減少＝リーク疑い |
| `min` | 起動後の最小空きヒープ | **これが下がり続けないこと**が最重要（リーク検出） |
| `temp` | ESP32-S3 内部ダイ温度(℃) | 発熱の上限。下記の目安参照 |
| `rssi` | Wi-Fi 電波強度(dBm) | -70 より悪いと切れやすい |
| `wifi` | 接続状態 OK/NG | |
| `drops` | Wi-Fi 切断回数(自動再接続込み) | 増え続けないこと |
| `state` | 再生状態 | 「再生中」が継続すること |
| `under` | スピーカPCM枯れ回数 | 増加＝音切れ |
| `starve` | 先読みFIFO枯れ回数 | 微増は許容。急増＝途切れ |
| `bat` | バッテリ残量% (`+CHG`=充電中) | 下記「バッテリ表示の注意」参照 |

---

## A. ヒートラン（連続再生・発熱/安定性）

1. 満充電後、USB を **挿したまま**（給電し続ける）。Wi-Fi 良好な場所に置く。
2. 起動 → 自動再生されることを確認。
3. そのまま **数時間〜一晩** 放置し、`[health]` を記録（モニタをログファイルに保存推奨）。
   ```
   pio device monitor | tee heatrun_YYYYMMDD.log        # PowerShell では Tee-Object
   ```
4. 判定（下記「合否基準」）。

> 通気の良い場所で。ケースに入れる場合は熱がこもりやすいので温度に注意。

## B. メモリリーク確認（長時間ソーク）

- A と同じ手順で、特に **`min`（最小空きヒープ）** を追う。
- 数時間で `min` が数十 KB 単位で下がり続ける場合はリーク。安定運用では `min` はほぼ一定に落ち着く。
- 参考: 番組名取得は 5 分ごと、局リスト取得は手動時のみ。これらの直後に `heap` が一時的に上下するのは正常。

## C. 選局スイッチング・ストレス（手動）

- `;`/`.` で局を選び `Enter` を繰り返し、数十回〜百回の選局切替を行う。
- 各切替で「バッファ中→再生中」に落ち着くこと、`under`/`starve` が異常増加しないこと、クラッシュしないことを確認。
- `space`（停止）↔ `Enter`（再生）の反復も行う。

## D. Wi-Fi 切断耐性（任意）

- ルーターを一時的に切る／離れる → `drops` が増え、復帰後に **自動再接続**して再生が戻ることを確認（DNS/NTP も自動再適用）。

---

## 合否基準（目安）

| 項目 | 合格の目安 |
|---|---|
| 連続稼働 | 数時間〜一晩、**再起動/フリーズなし** |
| メモリ | `min` が下げ止まり、一定レンジで安定（リークなし） |
| 温度 | `temp` が概ね **60℃未満**で頭打ち（ESP32-S3 の Tj 上限は 125℃だが、電池・筐体保護のため低めを推奨） |
| 音切れ | `under` が増えない。`starve` は微増可（連続再生が保てていればOK） |
| Wi-Fi | `drops` 後に自動復帰する。慢性的に増え続けない |

> クラッシュ時は `Guru Meditation`/`abort` などがログに出ます。その直前の `[health]`/`[play]`/`[seg]` 行が原因切り分けの手がかりになるので、ログごと共有してください。

---

## バッテリ表示の注意（重要）

本機のバッテリ残量は M5PM1（電源管理IC）の**電圧からの推定値**です。**負荷で電圧が沈む**ため、
- スイッチ ON（再生・Wi-Fi・アンプ稼働）＝電圧が下がり **低め%**（例 55%）
- スイッチ OFF / 充電中（負荷が軽い）＝電圧が戻り **高め%**（例 100%）

という **数十%の振れは正常** です（クーロンカウンタ方式ではないため）。USB 給電中は特に充電電圧に引っ張られて高く出ます。
Cardputer ADV は充電信号線が無く「充電中」を直接検出できません。そこで **電圧が高い（USB給電/満充電付近, 既定 `BAT_CHG_MV`=4100mV 以上）ときは % の代わりに `CHG`**（水色）を表示します。放電中のみ残量%を表示します。
なお `[health]` ログの `bat=` と `batmv=` は診断用に数値を出し続けます（CHG時は `bat` に `+CHG` を付記）。しきい値の調整は `src/config.h` の `BAT_CHG_MV` で行えます。
