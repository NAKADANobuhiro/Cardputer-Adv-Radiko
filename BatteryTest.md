# BatteryTest.md — バッテリー計測メモ（Cardputer ADV）

CardputerRadiko 実機（M5Stack **Cardputer ADV** / 1750mAh LiPo）での充電・放電の実測メモ。
残量は本機の仕様上「電池電圧(ADC)からの推定値」なので ±数% の揺れがある点に注意（数値は目安）。

---

## 1. 充電テスト（再生停止＋USB）

**条件**: `state=停止`（再生を止め、負荷が小さい状態）＋ 電源スイッチ ON ＋ USB 給電。
`[health]` ログの `up`（起動秒）と `bat`/`batmv` から算出。

| 区間 | up(秒) | bat | batmv | 時刻(参考) |
|---|---|---|---|---|
| 開始 | 4320 | 44% | 3650mV | 10:38 |
| 中間 | 10801 | 65% | 3836mV | 12:25 |
| 中間 | 13321 | 81% | 3942mV | 13:07 |
| 上端 | 16802 | 96% | 4068mV | 14:05 |
| 完了 | 17702 | **100%+CHG** | 4104mV | 14:20 |

### 結果（実測）

- **44% → 100% ＝ 13,382秒 ＝ 約3時間43分**
- 平均 **約15%/時（＝約4分で1%）**。電圧は 3650→4104mV までほぼ一定ペースで上昇。
- 電池1750mAh換算で 56%分＝約980mAh を3.72時間 → **実効 約260mA** の充電速度
  （本体が動作中＝停止再生でも WiFi 等を消費している分を差し引いた正味）。

### 0% → 100% の推測

- 実測ペース（約15%/時）を素直に伸ばすと 100 ÷ 15 ≈ **6.7時間**。
- ただし実際のリチウム充電は **下端（0〜10%）** と **上端（満充電付近のCV領域）** で遅くなるため、両端で余分にかかる。
- 総合して **0%→100% ≒ 約6.5〜7.5時間（目安 7時間前後）** と見るのが妥当。

> ※ これは「**再生停止（負荷小）＋USB**」での値。**再生しながら**だと消費と充電がほぼ拮抗して進みにくい。
> フル充電したいときは **再生停止（＋スイッチON）** が正解（M5公式も「充電時はスイッチON」）。

---

## 2. 放電テスト（電池駆動・連続再生）※参考

**条件**: USB を抜き、電池のみで radiko を連続再生（画面は無操作1分で消灯）。

- **57% → 約7% を 約7.7時間（`up≈27,845s`）で完走**。`under=0`（音切れゼロ）を維持したまま。
- 平均 約6〜7%/時の消費。
- 満充電からの単純外挿では **連続再生 約12〜14時間** が目安（放電カーブは非線形で終盤は急落）。

---

## 3. まとめ

| 項目 | 実測 | 満充電基準の推測 |
|---|---|---|
| 充電（停止＋USB） | 44%→100% を **約3時間43分** | 0%→100% **約7時間前後** |
| 放電（連続再生） | 57%→7% を **約7.7時間** | 100%→0% **約12〜14時間** |

- **充電のコツ**: 再生を止めて（負荷を下げて）USB接続。スイッチは **ON**（Cardputer ADV は充電時ON）。
- **残量表示の注意**: 電圧ベースの推定。USB接続中は充電レール電圧に引っ張られ、`batmv≥4100mV` で `CHG` 表示に切替。
- 計測は `[health]` ログ（`bat` / `batmv` / `up`）から。ログの読み方は `LogGuide.md` を参照。
